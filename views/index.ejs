<!DOCTYPE html>
<html lang="en" ng-app="Datablog">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <!-- DATADOG RUM -->
    <script type="text/javascript" src="https://www.datadoghq-browser-agent.com/datadog-rum-us.js" integrity="sha384-IYjwA4SyYSuGWv/BUWgCzXkRRJ+i9YhzDBbJGKUe+UKfzTEs8w/hgfU9ubdqzux5" crossorigin="anonymous"></script>
    <script nonce="<%= cspNonce %>">
        window.DD_RUM && window.DD_RUM.init({
            clientToken: '<%= rum.clientToken %>',
            applicationId: '<%= rum.applicationId %>',
            env: '<%= rum.env %>',
            service: '<%= rum.service %>',
            version: '<%= rum.version %>',
            allowedTracingOrigins: [/http:\/\/localhost:3000/],
            trackInteractions: true
        });
    </script>

    <!-- DATADOG BROWSER LOG COLLECTION -->
    <script type="text/javascript" src="https://www.datadoghq-browser-agent.com/datadog-logs-us.js" integrity="sha384-NjDY+szhvXY753BEcctHg6gkMRNU9fXhUo2X1XQ/dknITLaW8LjNt+xCHkw1YQWA" crossorigin="anonymous"></script>
    <script nonce="<%= cspNonce %>">
        window.DD_LOGS && DD_LOGS.init({
            clientToken: '<%= rum.clientToken %>',
            forwardErrorsToLogs: true,
            env: '<%= rum.env %>',
            service: '<%= rum.service %>',
            version: '<%= rum.version %>',
            sampleRate: 100
        });
        console.error("Test console error message")
        window.DD_LOGS && DD_LOGS.logger.info('Example DD_LOGGER message', {custom: { name: 'example json', id: 123 }});
    </script>

    <!-- Bootstrap 5 CSS -->
    <link href="/vendor/bootstrap.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <!-- Removed Google Fonts to satisfy CSP and reduce external domains -->
    <!-- Custom CSS -->
    <link href="/stylesheets/style.css" type="text/css" rel="stylesheet">
    
    <title>Welcome to Datablog</title>
</head>
<body ng-controller="MainController as vm">
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm mb-4">
        <div class="container">
            <a class="navbar-brand text-gradient" href="/">
                📝 Datablog
            </a>
            <div class="navbar-nav ms-auto">
                <div id="auth-nav">
                    <button class="btn btn-outline-primary" data-bs-toggle="modal" data-bs-target="#loginModal" id="loginBtn">
                        🔐 Login
                    </button>
                    <div id="userMenu" class="d-none">
                        <div class="dropdown">
                            <button class="btn btn-primary dropdown-toggle" type="button" data-bs-toggle="dropdown">
                                <span id="userEmail"></span>
                            </button>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="#" id="logoutBtn">Logout</a></li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <div class="container">
        <!-- Welcome Header -->
        <div class="page-header text-center">
            <h1 class="display-4 text-gradient mb-3">Welcome to Datablog!</h1>
            <p class="lead text-muted">Your personal blog powered by Datadog monitoring</p>
            <% if (typeof user !== 'undefined' && user) { %>
                <div class="mt-4">
                    <a href="/page" class="btn btn-primary btn-lg">
                        ✨ Create New Page
                    </a>
                </div>
            <% } %>
        </div>

        <!-- Pages Section -->
        <% if (typeof user !== 'undefined' && user) { %>
            <!-- Authenticated User - Show Pages Section -->
            <div class="page-content">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h2 class="mb-0">📚 Your Pages</h2>
                    <span class="badge bg-primary fs-6"><%= Object.keys(pages).length %> pages</span>
                </div>

                <% if (Object.keys(pages).length === 0) { %>
                    <!-- Empty State -->
                    <div class="text-center py-5">
                        <div class="mb-4">
                            <svg width="64" height="64" class="text-muted" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M4 4a2 2 0 012-2h8a2 2 0 012 2v12a2 2 0 01-2 2H6a2 2 0 01-2-2V4zm2 0v12h8V4H6z" clip-rule="evenodd"/>
                                <path d="M8 6h4v1H8V6zM8 8h4v1H8V8zM8 10h2v1H8v-1z"/>
                            </svg>
                        </div>
                        <h4 class="text-muted mb-3">No pages yet</h4>
                        <p class="text-muted">Get started by creating your first blog page!</p>
                        <a href="/page" class="btn btn-outline-primary">Create First Page</a>
                    </div>
                <% } else { %>
                <!-- Pages Table -->
                <div class="table-responsive">
                    <table class="table table-hover align-middle">
                        <thead>
                            <tr>
                                <th scope="col" class="text-center">#</th>
                                <th scope="col">Title</th>
                                <% if (typeof user !== 'undefined' && user && user.role === 'admin') { %>
                                <th scope="col">Author</th>
                                <% } %>
                                <th scope="col">Created</th>
                                <th scope="col" class="text-center">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% for (var p in pages) { %>
                                <tr>
                                    <td class="text-center">
                                        <span class="badge bg-light text-dark">#<%= pages[p]['id'] %></span>
                                    </td>
                                    <td>
                                        <a href="/page/<%= pages[p]['id'] %>" class="fw-semibold">
                                            <%= pages[p]['title'] || 'Untitled' %>
                                        </a>
                                    </td>
                                    <% if (typeof user !== 'undefined' && user && user.role === 'admin') { %>
                                    <td class="text-muted">
                                        <small>
                                            <i class="bi bi-person-circle"></i>
                                            <%= pages[p]['author'] ? pages[p]['author']['email'] : 'Unknown' %>
                                        </small>
                                    </td>
                                    <% } %>
                                    <td class="text-muted">
                                        <small><%= pages[p]['createdDate'] %></small>
                                    </td>
                                    <td class="text-center">
                                        <div class="btn-group btn-group-sm" role="group">
                                            <a href="/page/<%= pages[p]['id'] %>" class="btn btn-outline-primary">
                                                👁️ View
                                            </a>
                                            <a href="/page/<%= pages[p]['id'] %>/edit" class="btn btn-outline-secondary">
                                                ✏️ Edit
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
                <% } %>
            </div>
        <% } else { %>
            <!-- Non-Authenticated User - Show Login Prompt -->
            <div class="page-content">
                <div class="text-center py-5">
                    <div class="mb-4">
                        <svg width="64" height="64" class="text-muted" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M3 3a1 1 0 000 2v8a2 2 0 002 2h2.586l-1.293 1.293a1 1 0 101.414 1.414L10 15.414l2.293 2.293a1 1 0 001.414-1.414L12.414 15H15a2 2 0 002-2V5a1 1 0 100-2H3zm11.707 4.707a1 1 0 00-1.414-1.414L10 9.586 8.707 8.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                        </svg>
                    </div>
                    <h4 class="text-muted mb-3">🔐 Please log in to view your pages</h4>
                    <p class="text-muted mb-4">Sign in to access your personal blog pages and create new content.</p>
                    <button class="btn btn-primary btn-lg" data-bs-toggle="modal" data-bs-target="#loginModal">
                        🔑 Login to Continue
                    </button>
                </div>
            </div>
        <% } %>

        <!-- Footer -->
        <footer class="text-center py-4 text-muted">
            <small>Powered by Datadog monitoring • Built with ❤️</small>
        </footer>
    </div>

    <!-- Login Modal -->
    <div class="modal fade" id="loginModal" tabindex="-1" aria-labelledby="loginModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="loginModalLabel">🔐 Login to Datablog</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <form id="loginForm">
                        <div class="mb-3">
                            <label for="email" class="form-label">Email</label>
                            <input type="email" class="form-control" id="email" required>
                        </div>
                        <div class="mb-3">
                            <label for="password" class="form-label">Password</label>
                            <input type="password" class="form-control" id="password" required>
                        </div>
                        <div id="loginError" class="alert alert-danger d-none" role="alert"></div>
                        <div id="loginSuccess" class="alert alert-success d-none" role="alert"></div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="submit" form="loginForm" class="btn btn-primary" id="loginSubmit">
                        <span id="loginSpinner" class="spinner-border spinner-border-sm d-none me-2" role="status"></span>
                        Login
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap 5 JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Auth JavaScript -->
    <script src="/javascripts/auth.js"></script>
</body>
</html>
